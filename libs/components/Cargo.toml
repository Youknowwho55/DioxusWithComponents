[package]
name = "ui-components"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { version = "0.6", features = ["html"] }

[features]
default = []
showcase = []  # Optional dev features

[lib]
name = "ui_components"
path = "src/lib.rs"  # Create this file!